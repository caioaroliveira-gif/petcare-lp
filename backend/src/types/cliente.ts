export interface Cliente {
    id: string;
    nome: string;
    telefone: string;
    idade: number;
    email: string
}

 export interface CriarCliente {
    id: string;
    nome: string;
    telefone: string;
    idade: number;
    email: string;
}
